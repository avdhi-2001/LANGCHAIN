so basically we have to types of LLM 
one which can generate the structured output and that other which cant generate the structured output

1.) the one that generates the structured output uses "with atructurd output" which is  amethod in LLM that we
2.)th one that can not egnerate a structured output for them we have output parsers which is a class in LLm and it needs to be impoted and then it can help to generate the structured output

the output parserscan be used with both the on eehich can prodice the structured output and the other which can not

there are basically 4 types of structured output that we will study:


1.) stroutput:
2.) Jsonoutput
3.) structuredoutput
4.) pydanticoutput
